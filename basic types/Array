Arrays

let skill: string[];
skill = ['abc', 'def'];

console.log(skill[0]);
console.log(skill[1]);
skill.push("hi");

// Reassign new values
skill = ['abc', 'hi', 'def'];
skill.push(2);// throws error

// Accessing the first element and checking its type
const firstSkill = skill[0];
console.log(typeof(firstSkill));  // "string"

// Array of numbers
const series = [1, 2, 3];
console.log(series.length); // 3

output
Errors in code
Argument of type 'number' is not assignable to parameter of type 'string'.
[LOG]: "abc" 
[LOG]: "def" 
[LOG]: "string" 
[LOG]: 3 
________________________________________________________________________________________________________________________________________________________________________
//forEach
const array_of_number = [7, 8, 9];
array_of_number.forEach(function (value) {
    console.log(value);
});
output
[LOG]: 7 
[LOG]: 8 
[LOG]: 9 
________________________________________________________________________________________________________________________________________________________________________
//map()
const series_of_number = [1, 2, 3];
const doubleIt = series_of_number.map(e => e* 2);
console.log(doubleIt);
output
[ 2, 4, 6 ] 
________________________________________________________________________________________________________________________________________________________________________
//reducedSizee syntax 
//array.reduce(callback[, initialValue]);

const total = [0, 1, 2, 3].reduce(function(a, b){ return a + b; }); 
console.log("total is : " + total );

________________________________________________________________________________________________________________________________________________________________________
//filter()
//array.filter (callback[, thisObject]);

function isBigEnough(element, index, array) { 
   return (element >= 10); 
} 
          
let passed = [12, 5, 8, 130, 44].filter(isBigEnough); 
console.log("Test Value : " + passed );

let scores : (string | number)[];
scores = ['Programming', 5, 'Software Design', 4]; 

output
[LOG]: "Test Value : 12,130,44" 
